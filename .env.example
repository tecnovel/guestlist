# Database Connection
# Use Session Pooler (Port 5432) for both app and migrations
# This provides IPv4 compatibility and supports Prisma's prepared statements
DATABASE_URL="postgres://postgres.xxxx:password@aws-0-region.pooler.supabase.com:5432/postgres"

# Direct URL for migrations (same as DATABASE_URL for Prisma compatibility)
DIRECT_URL="postgres://postgres.xxxx:password@aws-0-region.pooler.supabase.com:5432/postgres"

# Next Auth
# You can generate a new secret on the command line with:
# openssl rand -base64 32
# https://next-auth.js.org/configuration/options#secret
AUTH_SECRET="your-secret-key"

# Base URL for the application
AUTH_URL="http://localhost:3000"
